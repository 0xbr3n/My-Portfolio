# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# Local dev / secrets — never publish (whole repo root is served by Cloudflare)
claude.md
*.backup
PGP-Passphrase.txt
.claude/
.vscode/
